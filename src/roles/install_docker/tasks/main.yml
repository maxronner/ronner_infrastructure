- name: Install Docker
  become: yes
  apt:
    name: docker.io
    state: present
    update_cache: yes

- name: Ensure Docker service is running
  become: yes
  systemd:
    name: docker
    state: started
    enabled: yes
