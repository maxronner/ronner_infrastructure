- name: Install Docker
  become: true
  apt:
    name: docker.io
    state: present
    update_cache: yes

- name: Ensure Docker service is running
  become: true
  systemd:
    name: docker
    state: started
    enabled: yes
